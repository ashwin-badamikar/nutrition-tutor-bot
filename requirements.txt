# FINAL REQUIREMENTS.TXT FOR DEPLOYMENT
# Based on your working Nutrition Tutor Bot

# Core Framework (REQUIRED)
streamlit>=1.28.0

# AI/ML Libraries (REQUIRED)
openai>=1.3.0
tiktoken>=0.5.0

# Vector Database (REQUIRED - Your app uses ChromaDB)
chromadb>=0.4.22
sentence-transformers>=2.2.2

# Data Processing (REQUIRED)
pandas>=2.1.0
numpy>=1.24.0
requests>=2.31.0
python-dotenv>=1.0.0

# Image Processing (REQUIRED for photo analysis)
pillow>=10.0.0

# Configuration (REQUIRED for settings)
pydantic>=2.5.0
pydantic-settings>=2.1.0

# Testing (OPTIONAL - for your tests)
pytest>=7.4.0
pytest-benchmark>=4.0.0

# Additional packages you installed during development
pathlib
json
logging
typing
time
base64
io

# Cloud deployment fixes
sqlite3